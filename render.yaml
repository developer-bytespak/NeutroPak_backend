services:
  - type: web
    name: neutro-backend
    env: node
    plan: free
    buildCommand: bash render-build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
